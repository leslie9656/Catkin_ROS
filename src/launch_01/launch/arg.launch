<launch>
    <!-- 需求:演示 arg 的使用,需要设置多个参数,这些参数使用的是同一个值 -->
    <!-- <param name='A' value= />
    <param name='A' value= />
    <param name='A' value= /> -->
    <arg name="car_length" value="0.55" />

    <param name="A" value="$(arg car_length)" />

    <param name="B" value="$(arg car_length)" />

    <param name="C" value="$(arg car_length)" />

</launch>